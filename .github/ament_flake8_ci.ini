[flake8]
# Baseline gate for release governance. The current codebase does not
# follow the full ament style profile yet, so block syntax errors and
# undefined-name style issues while leaving broader formatting cleanup to
# dedicated PRs.
select = E9,F63,F7,F82
max-line-length = 120
